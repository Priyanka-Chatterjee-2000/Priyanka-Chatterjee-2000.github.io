# ---------- Private working documents ----------
# IMPORTANT: these files contain employer-confidential / draft / personal
# material (private repo names, internal commit SHAs, LinkedIn export with
# phone & education history). Keep them OUT of the public repo.
resume-research.md
cv-draft.md
LinkedinProfile.pdf
# Local preview files (never commit)
preview-*.html

# ---------- LaTeX build artifacts ----------
*.aux
*.log
*.out
*.synctex.gz
*.toc
*.fdb_latexmk
*.fls
missfont.log

# ---------- Jekyll ----------
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/

# ---------- OS / editor ----------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
